# Normalize line endings: text files are stored with LF in the repo and
# checked out as LF on Linux (so `./script.py` shebangs work on the cluster).
* text=auto
*.py text eol=lf
*.md text eol=lf
*.yml text eol=lf
*.tif binary
